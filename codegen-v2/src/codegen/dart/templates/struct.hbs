// SPDX-License-Identifier: Apache-2.0
//
// Copyright © 2017 Trust Wallet.
//
// This is a GENERATED FILE, changes made here WILL BE LOST.
//

import 'dart:ffi';

import 'package:ffi/ffi.dart';
import 'package:generated_bindings.dart';
{{#if imports}}
{{#each imports}}
{{{this}}}
{{/each}}
{{/if}}

final class {{#unless is_public}}_{{/unless}}{{name}} {{~#if superclasses}} implements {{#each superclasses}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}{{/if}} {
    final TrustWalletCore _core;
{{#if init_instance}}
    final late {{{raw_type}}} _rawValue;

    {{{raw_type}}} get rawValue => _rawValue;

    {{name}}(TrustWalletCore core, {{{raw_type}}} rawValue)
        : _core = core, _rawValue = rawValue;
{{else}}

    const {{name}}(TrustWalletCore core): _core = core;
{{/if}}

{{! Equality operator, if available }}
{{#if eq_operator}}
    @override
    bool operator == (Object other) => switch(other) {
        {{name}} obj => _core.{{eq_operator.c_ffi_name}}(_rawValue, obj.rawValue),
        _ => false,
    };

    @override
    int get hashCode => _rawValue.hashCode;

{{/if}}
{{! Inits }}
{{#each inits}}
    {{#if has_defer}}
    {{~> partial_init_defer}}
    {{else}}
    {{~> partial_init}}
    {{/if}}

{{/each}}
{{! Deinits }}
{{#if deinits}}
    @override
    dispose() {
    {{#each deinits}}
        _core.{{name}}(rawValue)
    {{/each}}
    }

{{/if}}
{{! Methods }}
{{#each methods}}
    {{#if has_defer}}
    {{~> partial_func_defer}}
    {{else}}
    {{~> partial_func}}
    {{/if}}

{{/each}}
{{! Properties }}
{{#each properties}}
    {{~> partial_prop}}
    {{#unless @last}}
    {{/unless}}
{{/each}}
}
